# 改行は LF に正規化する。
# ★ Windows で編集したシェルスクリプトが CRLF のままコンテナに入ると、
#   entrypoint が "no such file or directory" で落ちる。
#   原因が改行だと気付くまで時間を溶かすので、リポジトリ側で固定しておく。
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.wav binary
*.mp3 binary
*.jar binary
